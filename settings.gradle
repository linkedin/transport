rootProject.name = 'standard-udfs'

def modules = [
 'standard-udfs-api',
 'standard-udfs-avro',
 'standard-udfs-examples',
 'standard-udfs-hive',
 'standard-udfs-presto',
 'standard-udfs-spark',
 'standard-udfs-type-system',
 'standard-udfs-utils'
]

 modules.each { module ->
  include "${module}"
}

