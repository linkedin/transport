//Build logic specific to OS environment, outside LinkedIn
//OS builds will use this build logic
logger.lifecycle("Configuring using default environment (defaultEnvironment.gradle)...")

subprojects {
  repositories {
    mavenCentral()
    maven {
      url "http://conjars.org/repo"
    }
  }
}

project.ext.setProperty('presto-version', '0.168')
project.ext.setProperty('airlift-slice-version', '0.28')
