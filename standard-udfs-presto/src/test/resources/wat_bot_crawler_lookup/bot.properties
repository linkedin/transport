# This is a comment. It must begin with # as the very first character on a line
# Empty lines like the one above (i.e. just a newline) are ignored; however, lines that are composed of of other
# whitespace are, unfortunately, not ignored.
#
# This defines a required exact match. The format is <token>,<exact> (NOTE: no whitespace or other characters in between
# the comma and the exact.
imustmatchexactly,exact
# This defines a regex match. The part that matches the regex pattern will be made available as the crawlerid field
^imustbeginwiththisprefix